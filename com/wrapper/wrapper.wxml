<!--com/wrapper.wxml-->
<view class="wrapper" wx:if="{{showWrapper}}" bindtap="hideWrapper">
    
</view>
<view class="panel {{panelBottom?'bottom':''}} {{panelCenter?'center':''}}">
    <view class="close_btn" bindtap="hideWrapper">
    </view>
    <slot />
</view>